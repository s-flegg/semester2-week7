Both files crash as the try to assign values outside the range of the list (they assume the list is longer than it is)

The pyton file is easier to debug as the error is explained and the exact line pointed out, compared to C which just gives a vague error.
